console.log('> Example 22');

function stringCheese() {
  console.log('> \t -> stringCheese:', SWISS);
  const SWISS = "full o' holes";
}
stringCheese();

// Uncaught ReferenceError: Cannot access 'SWISS' before initialization
