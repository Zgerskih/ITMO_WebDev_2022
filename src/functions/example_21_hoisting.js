console.log('> Example 21');

function stringCheese() {
  console.log('> \t -> stringCheese:', cheddar);
  let cheddar = 'sharp';
}
stringCheese();

// Uncaught ReferenceError: Cannot access 'cheddar' before initialization
